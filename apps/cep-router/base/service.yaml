apiVersion: v1
kind: Service
metadata:
  name: cep-router
  labels:
    app: cep-router
    tier: api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: cep-router
    tier: api
