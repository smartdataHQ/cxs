defaultNamespace: solutions
namespace: solutions

targetCustomizations:

  - name: production
    clusterSelector:
      matchLabels:
        role: production
    kustomize:
      dir: overlays/production