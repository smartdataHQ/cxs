apiVersion: apps/v1
kind: Deployment
metadata:
  name: contextapi
spec:
  template:
    spec:
      containers:
        - name: node # Name of the container to patch
          resources:
            requests:
              cpu: "150m"
              memory: "150Mi"
            limits:
              cpu: "300m"
              memory: "300Mi"
