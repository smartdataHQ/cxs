apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data

resources:
  - kafka.yaml

labels:
  - includeSelectors: true
    pairs:
      env: staging
      app.kubernetes.io/instance: kafka

