apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: solr-pdb
  namespace: data
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: solr
      app.kubernetes.io/instance: solr
      env: production
