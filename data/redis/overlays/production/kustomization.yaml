apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data

resources:
  - redis-stateful.yaml
  - redis-service.yaml
  - pdb.yaml
  - networkpolicy.yaml

labels:
  - includeSelectors: true
    pairs:
      env: production
      app.kubernetes.io/instance: redis


