apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: redis-pdb
  namespace: data
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: redis
      app.kubernetes.io/instance: redis
      env: production


