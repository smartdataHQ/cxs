resources:
  - ../../base

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patches:
  - target:
      kind: HelmChart
      name: redis
    patch: |-
      - op: replace
        path: /spec/valuesInline/service/annotations/tailscale.com~1hostname
        value: data-redis-dev
