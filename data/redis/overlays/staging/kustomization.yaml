resources:
  - ../../base

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patches:
  - target:
      kind: Service
      name: redis-master
    patch: |-
      - op: add
        path: /metadata/annotations/tailscale.com~1hostname
        value: data-redis-dev
