apiVersion: v1
kind: Service
metadata:
  name: neo4j
  labels:
    app: neo4j
    tier: data
spec:
  type: ClusterIP
  ports:
    - port: 7474
    - port: 7687
  selector:
    app: neo4j
    tier: data
