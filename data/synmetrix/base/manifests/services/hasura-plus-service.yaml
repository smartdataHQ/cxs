apiVersion: v1
kind: Service
metadata:
  name: synmetrix-hasura-plus
spec:
  selector:
    app.kubernetes.io/name: synmetrix-hasura-plus
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP