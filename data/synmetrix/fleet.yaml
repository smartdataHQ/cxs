defaultNamespace: synmetrix

kustomize:
  dir: ./overlays/production

targetCustomizations:
- name: production
  clusterSelector:
    matchLabels:
      env: production

dependsOn:
- name: data-apps
  selector:
    matchLabels:
      fleet.cattle.io/bundle-name: data-apps 