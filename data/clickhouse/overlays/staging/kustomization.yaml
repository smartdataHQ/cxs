apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data

resources:
  - ../production/clickhouse-stateful.yaml
  - ../production/clickhouse-service.yaml
  - ../production/clickhouse-config.yaml

labels:
  - includeSelectors: true
    pairs:
      env: staging
      app.kubernetes.io/instance: clickhouse
images:
  - name: clickhouse/clickhouse-server
    newTag: 25.7.4.11

