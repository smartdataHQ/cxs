defaultNamespace: data
namespace: data

targetCustomizations:

  - name: production
    clusterSelector:
      matchLabels:
        role: unused
    kustomize:
      dir: overlays/production

  - name: staging
    clusterSelector:
      matchLabels:
        role: staging
    kustomize:
      dir: overlays/staging