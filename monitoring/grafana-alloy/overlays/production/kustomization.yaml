resources:
  - ../../base/

namespace: grafana

helmCharts:
  - name: alloy
    repo: https://grafana.github.io/helm-charts
    releaseName: alloy
    # Find new versions
    #   helm repo add grafana https://grafana.github.io/helm-charts
    #   helm repo update
    #   helm search repo -l grafana/alloy
    version: 0.12.1
    namespace: grafana
    valuesFile: values.yaml