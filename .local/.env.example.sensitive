# .env.example.sensitive: Template for Customer-Specific Secrets (COMPLETE COVERAGE - UP TO DATE - Keys Only)
# Copy to .env.sensitive. Lists ALL sensitive vars from docker-compose.yml (latest) with empty values.
# Fill with provided/secure values (e.g., DOCKER_PAT from customer). Do NOT commit. Overrides .env.non-sensitive.
# Required marked; optional empty. Best practice: Generate with openssl (e.g., SECRET_KEY=$(openssl rand -base64 32)).
# Updated: Synced with all compose vars (e.g., added VOYAGE_API_KEY as DEFAULT_VECTOR_API_KEY alias).

# Docker Authentication (REQUIRED for image pulls)
DOCKER_REGISTRY=
DOCKER_USERNAME=quicklookup  # Fixed; do not change
DOCKER_PAT=

# Database Secrets (REQUIRED)
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_PASSWORD_LLM=

# Redis Secrets (REQUIRED)
REDIS_PASSWORD=

# AI/ML Service Keys (REQUIRED for AI/LLM features)
OPENAI_API_KEY=
VOYAGE_API_KEY=  # Used as DEFAULT_VECTOR_API_KEY
UNSTRUCTURED_API_KEY=

# Other API Keys (OPTIONAL)
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_API_BASE=
TAVILY_API_KEY=
FIRECRAWL_API_KEY=
HF_TOKEN=
GOOGLE_MAPS_API_KEY=

# Application Secrets (REQUIRED for signing/encryption/auth)
SECRET_KEY=  # 32+ chars random for JWT
TOKEN_SECRET_KEY=  # For access tokens
FERNET_KEY_PATTERN=  # Base64 for Fernet encryption (44 chars)
CONTEXT_SUITE_JWT_SECRET_KEY=  # If overriding default
CUSTOMER_BASED_SECRET=  # App-specific secret
EVENTS_SERVER_KEY=  # For events tracking

# OIDC Authentication (OPTIONAL for SSO; set NO_SSO=false in non-sensitive)
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OAUTH2_PROXY_COOKIE_SECRET=  # Base64 32 bytes random

# On-Prem Specific (OPTIONAL for partitioning/setup)
ONPREM_WRITE_KEY=
ONPREM_ORGANIZATION=
ONPREM_ORGANIZATION_GID=
ONPREM_PARTITION=

# SFTP/Integrations (OPTIONAL for file uploads)
SFTP_USERNAME=
SFTP_PASSWORD=

# User/DB (Non-secret but customer-set; defaults in non-sensitive)
REDIS_DB=0  # If overriding
