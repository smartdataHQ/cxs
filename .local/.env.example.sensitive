# .env.example.sensitive: Enhanced Template with Validation
#
# Format: @prompt:<step>|<required>|<type>|<description>|<default>
# Format: @validate:<rule>|<message>
# Format: @depends:<var>|<condition>
# Format: @skip (for fixed values)
# Format: @group:<name> (for logical grouping)
#
# Validation rules:
#   min-length:N - Minimum length
#   max-length:N - Maximum length
#   pattern:regex - Must match regex
#   exact-length:N - Must be exactly N chars

# @group:Docker Authentication
# Step 1: Docker Authentication
# @skip: Fixed value
DOCKER_REGISTRY=docker.io
# @skip: Fixed value
DOCKER_USERNAME=quicklookup
# @prompt:1|true|text|Docker Personal Access Token (provided to you)|
# @validate:pattern:^(ghp_|dckr_)|Warning: Docker PAT should start with 'ghp_' or 'dckr_'
DOCKER_PAT=

# @group:Database Configuration
# Step 2: Database Passwords
# @skip: Fixed value
CLICKHOUSE_USER=default
# @prompt:2|true|auto-generate-16|ClickHouse database password|AUTO_GENERATE:16
# @validate:min-length:8|Password should be at least 8 characters
CLICKHOUSE_PASSWORD=
# @prompt:2|false|password|ClickHouse LLM user password (press Enter to use main password)|${CLICKHOUSE_PASSWORD}
# @validate:min-length:8|Password should be at least 8 characters
# @depends:CLICKHOUSE_PASSWORD|always
CLICKHOUSE_PASSWORD_LLM=

# @prompt:2|true|auto-generate-16|Redis cache password|AUTO_GENERATE:16
# @validate:min-length:8|Password should be at least 8 characters
REDIS_PASSWORD=

# @group:AI/ML Services
# Step 3: AI/ML Service Keys
# @prompt:3|true|apikey|OpenAI API key (starts with sk-)|
# @validate:pattern:^sk-|Warning: OpenAI API key should start with 'sk-'
OPENAI_API_KEY=
# @prompt:3|false|apikey|Voyage AI embeddings key (uses local model if not provided)|
VOYAGE_API_KEY=
# @prompt:3|true|apikey|Unstructured.io document processing key|
UNSTRUCTURED_API_KEY=

# @group:Application Security
# Step 4: Application Security Keys
# @prompt:4|true|auto-generate-32|Main application secret key|AUTO_GENERATE:32
# @validate:min-length:32|Secret key should be at least 32 characters
SECRET_KEY=
# @prompt:4|true|auto-generate-24|Token signing key|AUTO_GENERATE:24
# @validate:min-length:24|Token secret should be at least 24 characters
TOKEN_SECRET_KEY=
# @prompt:4|true|auto-generate-44|Encryption key (Fernet)|AUTO_GENERATE:32
# @validate:exact-length:44|Fernet key must be exactly 44 base64 characters
FERNET_KEY_PATTERN=

# @prompt:4|false|text|Context Suite JWT secret (if overriding default)|
CONTEXT_SUITE_JWT_SECRET_KEY=
# @prompt:4|false|text|App-specific secret|
CUSTOMER_BASED_SECRET=

# @group:Optional Services
# Step 5: Optional API Keys
# @prompt:5|false|apikey|Tavily API key (for web search)|
TAVILY_API_KEY=
# @prompt:5|false|apikey|FireCrawl API key (for web scraping)|
FIRECRAWL_API_KEY=
# @prompt:5|false|apikey|Azure OpenAI key (if using Azure)|
AZURE_OPENAI_API_KEY=
# @prompt:5|false|text|Azure OpenAI endpoint (if using Azure)|
# @depends:AZURE_OPENAI_API_KEY|not-empty
AZURE_OPENAI_API_BASE=
# @prompt:5|false|text|HuggingFace token (for faster model downloads)|
HF_TOKEN=
# @prompt:5|false|apikey|Google Maps API key (if using location features)|
GOOGLE_MAPS_API_KEY=

# @group:On-Premises Setup
# Step 6: On-Prem Configuration
# @prompt:6|true|text|On-premises write key (provided for your setup)|
ONPREM_WRITE_KEY=
# @prompt:6|true|text|Organization name|
ONPREM_ORGANIZATION=
# @prompt:6|true|text|Organization group ID|
ONPREM_ORGANIZATION_GID=
# @prompt:6|true|text|Data partition identifier|
ONPREM_PARTITION=

# @group:File Integration
# Step 7: SFTP Integration
# @prompt:7|false|text|SFTP username for file uploads (if using SFTP)|
SFTP_USERNAME=
# @prompt:7|false|password|SFTP password|
# @depends:SFTP_USERNAME|not-empty
SFTP_PASSWORD=

# @group:Authentication
# Step 8: Single Sign-On
# @prompt:8|false|text|OIDC provider URL (e.g., https://your-auth0.auth0.com)|
OIDC_ISSUER_URL=
# @prompt:8|false|text|OIDC client ID|
# @depends:OIDC_ISSUER_URL|not-empty
OIDC_CLIENT_ID=
# @prompt:8|false|password|OIDC client secret|
# @depends:OIDC_ISSUER_URL|not-empty
OIDC_CLIENT_SECRET=
# @prompt:8|false|auto-generate-32|OAuth2 cookie secret|AUTO_GENERATE:32
# @depends:OIDC_ISSUER_URL|not-empty
OAUTH2_PROXY_COOKIE_SECRET=

# Non-secret configuration
# @skip: No prompt needed
REDIS_DB=0